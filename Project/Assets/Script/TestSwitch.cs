using UnityEngine;
using System.Collections;

public class TestSwitch : MonoBehaviour {

	// Use this for initialization
	void Start () {

	}
	
	// Update is called once per frame
	void Update () {

		/*if (Cardboard.SDK.CardboardTriggered) {
			if(GameObject.Find ("Plane").renderer.material.color == Color.red)
			{
				GameObject.Find ("Plane").renderer.material.color = Color.white;
			}
			else GameObject.Find ("Plane").renderer.material.color = Color.red;
		}*/
	}
}